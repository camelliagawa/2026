カード校正(自動検出)を廃止しA4専用化＋コード整理（死コード削除・命名整理）
RoboDK出力を単一ZIPに変更（複数DLブロック対策）＋README同梱
RoboDK用に包丁画像を出力（点群PLY＋テクスチャ平面OBJ／研磨ルートと同一座標系）
刃渡り曲線を検出エッジに忠実化（大域多項式を廃止し移動平均に変更）中央ズレ解消
シアン点の間引きと刃渡り(刃のみ)を弧長/弦基準に修正（斜め撮影で点が減る問題）
包丁画像オーバーレイのズレ修正（弦基準をアゴ→実切先に統一し終端に切先を含める）
3Dビューアに包丁エッジ画像を重ねて表示（アゴ・切先一致でCSV形状を目視検証）
刃先形状の補正ボタンを適用⇔キャンセルのトグル化（累積適用による形状崩れ防止）
3Dエッジ曲線をアゴ→切先の弦を基準に傾き補正（撮影時の傾きを除去しY軸に整列）
コード整理：死コード削除・メモリリーク修正・バグ修正・パフォーマンス改善
#99 刃先形状タブにθL/θR独立設定と分割アーク表示を追加（非対称断面対応）
#98 刃先形状xzグラフに等縮尺/自動の縦横比切り替えを追加
#72 resetApp()切り出し・drawCalibLabelインライン化・リセット時state漏れ修正
#71 旧手動計測モード完全削除・bladeEqualScaleをstate管理に・bladeDotIntervalでプレビュー更新
#70 コード整理：死コード削除・重複ヘルパー抽出・updateHint統合・不要コメント削除
#69 グラフに等縮尺モード追加（縦横同一スケール）・自動モードトグルボタン
#68 fetch-depth:0追加・バージョン番号を正しいコミット数に修正
#67 app.js・style.cssにSHAキャッシュバスト付与（ブラウザキャッシュ無効化）
#66 pages.yml Python1行化・バージョン情報が正しく埋め込まれるよう修正
#65 曲線プレビューのy軸を反転・エッジ画像と同じ向き（右上がり）に修正
#64 バージョン情報をCI埋め込みに変更（fetch失敗時も消えない）
#63 刃渡り曲線プレビューをモーダルからボタン直下インライン表示に変更
#62 刃渡り曲線CSV：プレビューモーダル追加・6列(x y z rx ry rz)出力に変更
#61 カメラ映像エリアをカメラ起動時のみ表示・停止時は非表示
#60 エッジ画像枠にボタンを操作順①②③で統合（キャリブ→アゴ切先→CSV）
#59 不要ボタン削除（エリア指定検出・長辺2点校正）と死コード除去（drawMeasLine, gaussianSmoothArr, ROI/CalibTap関連）
#58 アゴ・切先指定後もカード校正オーバーレイを維持（ブレード再描画時に再重ね）
#57 検出画像の刃渡り・柄・カード長辺ラベルを非表示・確認ダイアログの刃渡り値を非表示
#56 カード校正点①②③をドラッグで調整可能に・確定時に曲線を新校正値で再計算
#55 カード校正オーバーレイ改善：①②直線を延長表示・直角マーク・ラベル位置改善
#54 エッジ画像でカード短辺3点指定→垂線距離85.6mmで再校正・やり直しボタン付き
#53 カード長辺85.6mmをCARD_LONG_MM定数に集約（ISO/IEC 7810 ID-1規格明記）
#52 曲線長（アゴ〜切先の弧長）を計算・計測結果に表示・ドラッグ更新にも反映
#51 ノイズ除去デフォルトを0に変更（白塗りバグ回避）・スライダーで任意調整可能
#50 ノイズ除去をconnectedComponentsWithStats方式に修正（包丁白塗りバグ解消）・デフォルト80px²に戻す
#49 ノイズ除去閾値・赤丸サイズをUIスライダーで調整可能に（詳細設定に追加）
#48 赤丸縮小・エッジ画像のノイズ点除去（面積80px未満の輪郭を削除）
#47 手動指定のみに統一：自動検出削除・赤丸ドラッグで白線上を移動しリアルタイム更新
#46 手動アゴ・切先指定：エッジ画像クリックで2点指定→白線トレース→滑らか曲線表示
#45 刃渡り曲線：二次多項式フィットで滑らか化＋エッジ画像から自動矩形検出
#44 刃渡り曲線：アゴ・切先閾値を15%→8%に引き下げ（刃渡り160mm→180mm近似へ）
#43 刃渡り曲線：両側スキャン＋最大傾き始点でアゴ検出（刃側自動判定）
#42 刃渡り曲線：アゴ＝20%閾値（柄水準起点）・切先＝20%閾値に精度改善
#41 刃渡り曲線：アゴ＝最大傾き・切先＝30%閾値で幾何学特徴ベース検出に変更
#40 刃渡り曲線：アゴ・切先の赤点を大型化・白リング輪郭・ラベル黒縁追加
#39 刃渡り曲線：アゴ・切先に赤点＋ラベル表示を追加
#38 刃渡り曲線：ピクセルスキャン開始点をbbox外側(×1.1)に修正（内側スキャンのずれ解消）
#37 刃渡り曲線：エッジ画像ピクセル直接スキャンに変更（輪郭点プロファイル廃止）
#36 刃渡り曲線：写真からシアン線削除・エッジ画像のみに描画（描画順も修正）
#35 刃渡り曲線：エッジ検出画像にもシアン曲線表示・CSV出力をドット間隔でサンプリング
#34 刃渡り曲線：アゴ検出をbottomEdge閾値(50%段差)に変更・エッジ画像表示バグ修正
#33 刃渡り曲線：コーナー検出廃止・2階微分でアゴ検出+エッジ検出画像を結果タブに表示
#32 刃渡り曲線：アゴコーナー選択を「刃側に最も近い」→「柄側に最も近い」に修正
#31 刃渡り曲線：アゴ検出を contour の直角コーナー検出ベースに変更
#30 刃渡り曲線：アゴ検出を刃線（下端Y）の角度急変ベースに変更
#29 刃渡り曲線：アゴ検出を最大幅勾配ベース（ボルスター遷移検出）に置換
#28 刃渡り曲線：アゴ検出を刃元から柄方向へ80%閾値スキャンに変更（精度改善）
#27 刃渡り曲線：刃側を常にsmMaxY（下端）固定・アゴをタング最小点に直接設定
#25 刃渡り曲線：刃/棟判定を輝度勾配から幾何判定（先端〜アゴの移動量比較）に変更
#24 刃渡り曲線：アゴ検出を「柄端スキップ＋タング最小点から刃元へ閾値」に刷新
#23 刃渡り曲線：アゴ検出を柄幅適応型閾値に変更（柄幅に依存しない）
